const config = {
    path: {
        base: "demoFolder",
    },
    filter: {
        fileExtensionFilter: "*.pdf",
        fileNameRegexp: /(fel)(\s*)(\d+)(\s*)\.(\s*)pdf(\s*)$/i
    }
}

module.exports = config